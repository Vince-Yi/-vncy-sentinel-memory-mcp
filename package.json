{
    "name": "@vncy/sentinel-memory-mcp",
    "version": "0.1.0",
    "description": "Lightweight MCP server that records and reuses Prompt Gaps",
    "type": "module",
    "bin": {
        "sentinel-memory-mcp": "./dist/server.js"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "start": "node dist/server.js",
        "prepublishOnly": "npm run build"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.0.0",
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
